# Start Playphrase.me Site Specification Migration

Target repository:
`/Users/eugenepotapenko/Projects/playphrase.me/playphraseme-site`

Canonical workflow repository:
`/Users/eugenepotapenko/Projects/potapenko-github/spec-first-bootstrap`

## Objective

Convert the large site specification library into a selective Markdown-first
tree without changing product behavior, implementation, QA evidence, market
authority, operational policy, or production state.

## Mandatory pre-action boundary

1. Work only in the target repository and current branch. Do not create a
   branch or worktree.
2. Re-read active global instructions and target `AGENTS.md`.
3. Read `docs/specs/README.md` only as the initial legacy registry. Do not
   follow every listed product contract or support document.
4. Read only the Bootstrap migration contract,
   `docs/specs/legacy-migration-routing.md`,
   `prompts/migrate-legacy-spec-library.md`, referenced templates, and helper
   usage.
5. Do not inspect Clojure/ClojureScript source, tests, QA case bodies, runtime,
   browsers, logs, MongoDB, object storage, remote hosts, or production during
   census and checkpoint-0 planning.
6. Do not change code, behavior, global configuration, deployment, DNS, data,
   or unrelated files.

## Revalidate the snapshot

The planning snapshot was 90 Markdown documents, 138,717 words, 20,554 lines,
and 64 documents over 100 lines. The existing `docs/specs/README.md` root was
177 lines. There were no declared Markdown nodes, JSON spec state, or migration
directory. Repeat the mechanical census without printing bodies or a complete
inventory.

## Protected authority

Keep product contracts distinct from plans, runbooks, goal registries,
operator handoffs, QA workflows, design references, and evidence logs. Preserve
market-specific precedence and epochs for Russia, China, India, and Brazil;
do not merge regional authority into a generic contract. Production and
launched regional servers remain operator-only. Active goal state and accepted
handoffs must remain restartable and reachable without becoming product intent.

## Checkpoint 0 — plan and stop

Before writes, present the current branch, exact write set, a root below 100
lines, domain branch structure, dependency links, source dispositions, and
protected market/operational boundaries. Select one low-conflict pilot batch of
3 documents or 12,000 source words; one larger document may stand alone. Do not
choose an active goal registry or operator handoff as the pilot unless the user
explicitly requests that domain. Include verification.
Wait for explicit approval.

## After approval

Create only Markdown state under `docs/specs/migration/`: `README.md`, one
active batch node, and one receipt. Do not create JSON routing, inventory,
mapping, or authority files.

Process only the approved batch. Read its sources and explicit dependencies,
preserve precedence, clause IDs, provenance, inbound links, and source roles.
Every node must be at most 100 physical lines, preferably 50–80. Validate links,
reachability, coverage, drift, and no JSON routing state. Do not start another
batch in the same checkpoint.

Finish with a scoped commit and push from the currently checked-out branch.
Report compact counts, node links, protected authority, residuals, and the next
batch; never return corpus bodies.
