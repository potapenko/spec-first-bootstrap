# Set Up The Complete Bootstrap Globally

Use https://github.com/potapenko/spec-first-bootstrap as the canonical source.

Configure all three Bootstrap layers in the current user's global agent
environment so they are available across future projects. Change no project
repository during installation.

This is one advanced global setup task with three ordered phases. Read and
execute each detailed contract completely:

1. **Specifications:** `prompts/setup-global-spec-first.md`.
2. **Agents:** `prompts/setup-global-agents.md`.
3. **QA:** `prompts/setup-global-browser-qa.md`.

Resolve the active agent environment and its supported user-level instruction
entry point once. Do not assume another user's path or silently configure more
than one agent product.

Before writing outside the current project:

1. state the exact global directory and target files;
2. explain which existing instruction sections will be merged or reconciled;
3. request only the filesystem permission required for those paths;
4. stop if the target mechanism or scope is ambiguous.

Preserve all existing global instructions. Keep full governance documents
conditionally loaded. Do not copy project-specific product, framework, safety,
build, test, database, storage, Git, or release rules into the global layer.

Do not create, resume, pause, block, or complete a persistent goal. Do not
change model defaults, reasoning defaults, concurrency limits, custom agent
profiles, providers, browser profiles, sessions, or application configuration.

Use `apply_patch` for edits. Do not ask the user to run the three phase prompts
separately.

Before finishing, verify:

- exact global scope and detected agent environment;
- all three phase dispositions;
- one non-duplicated global instruction hierarchy;
- canonical governance documents and conditional routing;
- instruction-size limits, Markdown, and whitespace;
- no project-repository, goal-state, model, browser-session, or application-
  configuration change.

Report exact global paths, permission used, installed layers, merge decisions,
verification, and residuals. Do not create a Git commit for configuration files
outside a Git repository.
